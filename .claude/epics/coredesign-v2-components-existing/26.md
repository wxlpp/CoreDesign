---
name: BottomInputBar + MenuButton 重构 + iOS build fix（spike）
status: open
created: 2026-05-09T22:32:29Z
updated: 2026-05-09T23:21:43Z
github: https://github.com/wxlpp/CoreDesign/issues/26
depends_on: []
parallel: true
conflicts_with: []
---

# Task: BottomInputBar + MenuButton 重构 + iOS build fix（spike）

## Description

把 `Sources/CoreDesign/Components/BottomInputBar/{BottomInputBar,MenuButton}.swift` 按 v2-tokens 重构（最高频 magic-number 集中地，~15 处带-edge padding 在 BottomInputBar.swift），**保留**白名单内的 `.glassEffect` 用法（输入栏 pill 自身 + suggestion chip pill + shuffle 浮按钮 + MenuButton 自身的 menu/circular 浮按钮）。

**额外硬交付**（PRD US-4 / FR-A-6 / SC#5 必修，不是降级出口）：修 `MenuButton.swift` 在 iOS 上的 `UIImpactFeedbackGenerator` main-actor build 错误。

**Soft sequencing**：本 task 用 `.buttonStyle(.circularGlass)` 渲染 suggestion / send / stop 按钮（line 143-167），该 style 由 Task 1 owns；功能上无硬依赖，但视觉验收建议在 Task 1 落 main 之后做最终抽查。

## Acceptance Criteria

### Token 化
- [ ] `BottomInputBar.swift` 所有带-edge padding（`.padding(.horizontal, 12)` / `.padding(.vertical, 8)` 等 ~15 处）→ `CoreSpacing.*`
- [ ] `BottomInputBar.swift` cornerRadius / radius 字面量（如 `BottomInputBarGlassEffectShape` 内 `cornerRadius: 14`）→ `CoreRadius.large` 或对应 token
- [ ] `BottomInputBar.swift` 字号 / spacing / icon size 字面量 → `CoreTypography.*` / `CoreSpacing.*` / `CoreControlMetrics.iconSize(for:)`
- [ ] `MenuButton.swift` 同样 token 化 padding / cornerRadius / 字号

### Glass 策略保留（按 epic ADR #3）
- [ ] `BottomInputBar.swift:131` 输入栏 pill 自身的 `.glassEffect` **保留**
- [ ] `BottomInputBar.swift:259-267` suggestion chip pill 的 `.glassEffect` **保留**
- [ ] `BottomInputBar.swift:334-342` shuffle 浮按钮的 `.glassEffect` **保留**
- [ ] `MenuButton.swift:91, 105` 浮按钮的 `.glassEffect` **保留**
- [ ] 完成后 `grep -rln 'glassEffect' Sources/CoreDesign/Components/BottomInputBar/` 命中文件 ⊆ {`BottomInputBar.swift`, `MenuButton.swift`}

### iOS build fix（必修，非降级）
- [ ] `MenuButton.swift` 的 `UIImpactFeedbackGenerator` main-actor 错误已修；scope 从 `init / prepare / impactOccurred` 整段（line 168-173 附近的 helper）开始判断，不是只包 `impactOccurred()` 一行
- [ ] 选 PRD 列出的 3 条修复路径之一并在 PR description 说明：
  - (a) helper 整体 `@MainActor` 隔离
  - (b) 调用处 `Task { @MainActor in generator.impactOccurred() }` 异步派发
  - (c) **直接移除 feedback 调用**（最低风险路径；如非关键交互可选）
- [ ] **若 0.5d spike 内 (a)/(b) 两条都不通，则用 (c)**——这不是设计回退，是 PRD 允许的修复方式之一

### Magic-number 归零
- [ ] `BottomInputBar.swift` 三条 grep 命中 = 0（排除 `#Preview`）
- [ ] `MenuButton.swift` 三条 grep 命中 = 0

### Build / test / preview
- [ ] `swift build -Xswiftc -warnings-as-errors` (macOS) 通过 0 warning
- [ ] `swift build -Xswiftc -warnings-as-errors --triple arm64-apple-ios26.0-simulator` 通过 0 warning（**关键**：本 task 验收的核心标志）
- [ ] `swift test` 退出 0
- [ ] `#Preview` light / dark 双模式 + 键盘弹出 / 收起 / suggestion 切换 / 长文本展开 都无 regression

### Soft sequencing 视觉验证
- [ ] PR description 注明：本 task 用 `.buttonStyle(.circularGlass)`；如 Task 1 (Button styles) 改了 `CircularGlassButtonStyle` 视觉，本 PR 视觉描述基于"当前 main 上的 Style 实现"；Task 1 落 main 后视觉自动继承，不需本 task 改代码

### PR description 5 字段
- [ ] Light preview / Dark preview / 视觉变化摘要 / Glass 变化（必须明示哪 4 处保留 glass + 修复路径选哪个）/ public API diff 全部填写
- [ ] doc-comment 完整性勾选确认（`BottomInputBar` 内的 `init` 注释保留 + `BottomInputBarSuggestionsView` / `BottomInputBarModifier` 长 doc-comment 保留）

## Technical Details

参考 epic.md `### 输入栏 + 修 build 错（FR-A-6）`。本组件结构最复杂；现有代码已有大量 inline 设计文档（`autoFocus` / `externalFocus` / `safeAreaBar` 注释），重构时**保留**这些设计说明，仅替换字面量。

`BottomInputBarGlassEffectShape` 内的 `cornerRadius` 取值条件（高度 ≤ 44 走 `height/2`，否则 14）的逻辑保留；只把 `14` 字面量替换为 `CoreRadius.large` 或类似 token。

## Dependencies

- v2-tokens 全部 merged（已满足）
- Soft sequencing on Task 1（功能上 depends_on: []，视觉验收上推荐在 Task 1 后）

## Effort Estimate
- Size: XL
- Hours: 12 - 20（1.5 – 2.5 天，含 iOS build fix spike）

## Definition of Done

- [ ] Acceptance Criteria 全部勾选
- [ ] iOS + macOS 双 SPM build 全绿（**双绿**才算 done）
- [ ] PR opened with 5 字段 description
- [ ] PR review + merge 到 main
