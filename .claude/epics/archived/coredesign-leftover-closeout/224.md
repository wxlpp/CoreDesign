---
name: 补两处测试断言（SegmentedControl / StateLabel）
status: open
created: 2026-09-02T15:13:50Z
updated: 2026-09-02T15:28:35Z
github: https://github.com/wxlpp/CoreDesign/issues/224
depends_on: []
parallel: true
conflicts_with: []
---

# Task 005 · 补两处测试断言

覆盖 PRD FR-8。

⚠️ **本 task 没有「跑绿」这个交付判据**——epic Success #6「`swift test` 全绿」在本 task 什么都不做时照样通过。判据见下方 DoD。

## 1 · `SegmentedControlTests.plainStyleOptsOutOfGlass`

`Tests/CoreDesignTests/SegmentedControlTests.swift:35-45`，函数体末尾是 `_ = styled`——**纯编译检查，无运行时断言**，却顶着一个承诺行为的名字。

**处置（二选一，不得两头不靠）**：
- **A**：补上真运行时断言（验证 plain style 确实没有 glass 效果）。
- **B**：若受 `Text` 类型擦除限制无法直接断言，**如实改名**（如 `plainStyleCompilesWithFourPieceWiring`）**并在注释里写明它只能做到什么**。

## 2 · `StateLabelTests.customLabelPreservesStyle`

`Tests/CoreDesignTests/StateLabelTests.swift:38-42`，只断言 `label.style`，**没有任何断言触及 label payload**——而测试名承诺的正是「custom label」被保留。

**处置**：补上对 label payload 的断言（自定义 label 确实被 wiring 到渲染路径）。同样，若类型擦除挡住，如实降级命名并说明。

## Definition of Done

- [ ] 两个测试**各自**：要么有真运行时断言，要么已如实改名 + 注明能力边界
- [ ] 若走改名路线，注释明确写出「为什么断不了」而非含糊带过
- [ ] `swift test` 不低于 `baseline.md` 记录的 454（补断言后应 ≥ 454）
