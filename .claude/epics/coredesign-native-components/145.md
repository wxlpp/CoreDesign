---
name: 文档与发版 0.4.0
status: open
created: 2026-07-23T04:33:46Z
updated: 2026-07-23T04:33:46Z
github: https://github.com/wxlpp/CoreDesign/issues/145
depends_on: [144]
parallel: false
conflicts_with: []
---

# Task: 文档与发版 0.4.0

## Description

Phase 2 收尾。与 Phase 1 不同,本 epic 基本是纯新增,破坏面小——唯一的破坏性变更是 #140 的 `.content`/`.card` 指向修改(同名换值)。

## Acceptance Criteria

### BREAKING-CHANGES

- [ ] `docs/BREAKING-CHANGES.md` 新增 `0.4.0` 条目
- [ ] 「同名换值」记录 `.content` / `.card` 的 `SurfaceKind` 背景指向变更(`surfaceCard`/画布色 → `surfaceRaised`)——对下游编译零感知,但会改变 `.surface(.content)` 的观感
- [ ] **落实 Phase 1 里标注「将于 0.4.0 提供」的替代物**:回到 `0.3.0` 条目,把当时挂账的替代品指向本次交付的组件(注意 6 个被删的 GitHub 组件 Phase 1 已明确是「无直接替代」,不要在这里硬造映射——只落实真正承诺过的)
- [ ] 若 #140 收敛了 `SurfaceKind` 档位,一并记录

### 文档与快照

- [ ] `docs/DESIGN-FOUNDATION.md` 补新组件相关的设计依据(若有新 token / 新约定)
- [ ] `docs/README.md` 组件索引新增本 epic 的组件(Card / Separator / SectionHeader / SectionFooter / InsetGroupedSection / SettingsRow / 5 个 style),组件计数更新
- [ ] `scripts/run-snapshots.sh` 为新组件生成快照
- [ ] 新组件的 `docs/components/*.md` 页(与既有惯例一致)

### 项目指令与版本

- [ ] `CLAUDE.md` / `AGENTS.md`:若新组件引入了新的架构约定(如 `.core` style 的 `.tint` 通路惯例),补进去;两文件逐行同步
- [ ] `README.md` 版本 pin `0.3.0` → `0.4.0`
- [ ] 下游编译探针跑通;若新增了值得下游验证的公开符号,评估是否加进 `scripts/downstream-probe`
- [ ] **打 `0.4.0` tag 并发 release**——但这是对外动作,与 Phase 1 一样**留给编排在 epic 合入 main 前由用户确认**,本任务只把版本 pin 与文档改好

## Technical Details

「落实 0.4.0 替代物」这条要小心:Phase 1 的 `BREAKING-CHANGES.md` 里凡标了「将于 0.4.0 提供」的,现在要能指向真实交付的组件;凡标了「无直接替代」的(6 个 GitHub 时间线组件),**不要**因为 Phase 2 交付了通用容器就反过来硬说它们是替代——`Card` / `InsetGroupedSection` 是通用容器,不是那些场景组件的等价物(Phase 1 已裁决)。

## Dependencies

依赖 #144(文档以视觉终审后的终态写)。

## Effort Estimate
- Size: M
- Hours: 8

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
