---
name: BookCover 重构
status: open
created: 2026-05-09T22:32:29Z
updated: 2026-05-09T23:21:43Z
github: https://github.com/wxlpp/CoreDesign/issues/27
depends_on: []
parallel: true
conflicts_with: []
---

# Task: BookCover 重构

## Description

把 `Sources/CoreDesign/Components/BookCover/BookCover.swift` (~133 行) 按 v2-tokens 重构。当前文件含 `cornerRadius: 8` / `lineWidth: 0.5` / 阴影参数 `radius: 6, x: 0, y: 3` 等字面量。**不使用 `.glassEffect`**。

## Acceptance Criteria

### Token 化
- [ ] `RoundedRectangle(cornerRadius: 8)`（封面 clipShape + stroke overlay）→ `CoreRadius.large`（值 8 与 Primer borderRadius.large 一致）
- [ ] `lineWidth: 0.5` → `CoreBorderWidth.hairline`
- [ ] 阴影 `Color.black.opacity(0.18), radius: 6, x: 0, y: 3` → `View.coreShadow(.medium)` 或 `CoreElevation.medium`（视觉 dark mode 下 colorset 提供 light/dark 自适应阴影）
- [ ] `Color.black.opacity(0.08)` stroke → `Color.borderMuted` 或视觉抽查决定
- [ ] `BookCoverPlaceholder` 内的字号 / padding / spacing 字面量 → `CoreTypography.*` / `CoreSpacing.*`
- [ ] `Color(text: String)` 算法生成色保留（这是颜色生成逻辑，不是 magic-number）

### Glass 策略
- [ ] **不**使用 `.glassEffect`

### Magic-number 归零
- [ ] `BookCover.swift` 三条 grep 命中 = 0（排除 `#Preview`）

### Build / test / preview
- [ ] `swift build -Xswiftc -warnings-as-errors` 双平台通过
- [ ] `swift test` 退出 0
- [ ] `#Preview` light / dark 双模式 + 有图 / 无图（fallback placeholder）两种态视觉合理

### PR description 5 字段
- [ ] 5 字段全部填写
- [ ] doc-comment 完整性勾选确认（`BookCover` + `BookCoverPlaceholder` 两个 public struct）

## Technical Details

参考 epic.md `### 容器 / 内容显示`。`BookCover.aspectRatio` 静态常量保留（`2.0 / 3.0` 是书籍封面行业标准比例，不是 magic number）。

跨平台 `image(from data: Data) -> Image?` 内 `#if canImport(UIKit) / AppKit` 分支保持不动。

## Dependencies

- v2-tokens 全部 merged（已满足）

## Effort Estimate
- Size: S
- Hours: 4（0.5 天）

## Definition of Done

- [ ] Acceptance Criteria 全部勾选
- [ ] PR opened with 5 字段 description
- [ ] PR review + merge 到 main
