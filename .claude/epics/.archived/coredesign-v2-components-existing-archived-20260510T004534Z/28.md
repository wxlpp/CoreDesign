---
name: Avatar + CheckBox 重构（合并）
status: open
created: 2026-05-09T22:32:29Z
updated: 2026-05-09T23:21:43Z
github: https://github.com/wxlpp/CoreDesign/issues/28
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Avatar + CheckBox 重构（合并）

## Description

把 `Sources/CoreDesign/Components/{Avatar,CheckBox}/` 两个小文件按 v2-tokens 重构。**合并理由**：两文件各 ~50 行（Avatar 42 行 / CheckBox 42 行），都很小、互不依赖、互不共享 type / state；合并是 small-file bundling 控制 task 数量，**不是**技术耦合。

**不使用 `.glassEffect`**（两者都不在白名单）。

## Acceptance Criteria

### Avatar.swift 修改
- [ ] 圆角（如 `Circle()` / `RoundedRectangle(cornerRadius: ...)`）保留圆形语义，但任何 cornerRadius 字面量 → `CoreRadius.full`
- [ ] 字号 / padding / 内边距 / 描边 width 字面量全部 token 化（`CoreTypography.*Font` / `CoreSpacing.*` / `CoreBorderWidth.*`）

### CheckBox.swift 修改
- [ ] `Image(systemName:)` icon size（如有 inline）→ `CoreControlMetrics.iconSize(for:)`
- [ ] padding / spacing 字面量 → `CoreSpacing.*`
- [ ] checkmark color / unchecked color 用现有 `Color.primary` / `Color.gray` 保持兼容（不动颜色映射，本 task 仅做 token 化 padding/size）

### Glass 策略
- [ ] 两文件都**不**使用 `.glassEffect`

### Magic-number 归零
- [ ] `Avatar.swift` 三条 grep 命中 = 0（排除 `#Preview`）
- [ ] `CheckBox.swift` 三条 grep 命中 = 0

### Build / test / preview
- [ ] `swift build -Xswiftc -warnings-as-errors` 双平台通过
- [ ] `swift test` 退出 0
- [ ] `Avatar` / `CheckBox` 各自 `#Preview` light / dark 双模式视觉合理

### PR description 5 字段
- [ ] 5 字段全部填写
- [ ] doc-comment 完整性勾选确认（`Avatar` + `CheckBox` + `CheckBoxToggleStyle` 等 public 表面）

## Technical Details

参考 epic.md `### 容器 / 内容显示`。本 task 是 epic 内最简单的之一（合并后估时仍仅 0.5d）。

`CheckBoxToggleStyle: ToggleStyle` 形态保持不变（与 Apple `ToggleStyle` 协议 alignment）；只替换 makeBody 内字面量。

## Dependencies

- v2-tokens 全部 merged（已满足）

## Effort Estimate
- Size: S
- Hours: 4（0.5 天）

## Definition of Done

- [ ] Acceptance Criteria 全部勾选
- [ ] PR opened with 5 字段 description
- [ ] PR review + merge 到 main
