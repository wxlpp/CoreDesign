---
name: Banner 重构
status: open
created: 2026-05-09T22:32:29Z
updated: 2026-05-09T23:21:43Z
github: https://github.com/wxlpp/CoreDesign/issues/22
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Banner 重构

## Description

把 `Sources/CoreDesign/Components/Banner.swift` 按 v2-tokens 重构。`PlainBannerStyle` / `BorderedBannerStyle` 两个 `BannerStyle` 实现的 padding / background / border 全部走 token；按 PRD §US-3 + epic ADR #3 **不使用 `.glassEffect`**（Banner 是基础容器，不在白名单内）。

## Acceptance Criteria

### Token 化
- [ ] `PlainBannerStyle.makeBody`：`.padding()` → `CoreSpacing.*`（建议 `CoreSpacing.md` for 内边距）；`Rectangle().fill(palette.background)` 保留，但取值层走 `Color.{info,warning,danger,success}Background` 命名（已存在）
- [ ] `BorderedBannerStyle.makeBody`：同上；`.bordered(style: palette.border)` 保留，但 border width 显式 `CoreBorderWidth.thin`（如 `BorderModifier` 默认值已是 `CoreBorderWidth.thin`，确认无需 override 即可）
- [ ] icon padding / spacing 走 `CoreSpacing.*`
- [ ] 字号（如有 inline 字面量）走 `CoreTypography.bodyMediumFont`

### Glass 策略
- [ ] **不**使用 `.glassEffect`（`Banner.swift` 不在 ADR #3 白名单）

### Magic-number 归零（按 PRD SC#1）
- [ ] `Banner.swift` 三条 grep 命中 = 0（排除 `#Preview`）

### Build / test / preview
- [ ] `swift build -Xswiftc -warnings-as-errors` 双平台通过 0 warning
- [ ] `swift test` 退出 0
- [ ] `#Preview` light / dark 双模式 4 种 `MessageLevel`（info / warning / danger / success）+ Plain / Bordered 两种 style 视觉合理

### PR description 5 字段（按 epic ADR #6）
- [ ] Light preview / Dark preview / 视觉变化摘要 / Glass 变化 / public API diff 全部填写
- [ ] doc-comment 完整性勾选确认（`Banner` / `BannerStyle` protocol / `BannerStyleConfiguration` / `MessageLevel` / `PlainBannerStyle` / `BorderedBannerStyle` / `EnvironmentValues.bannerStyle` / `View.bannerStyle(_:)` 共 8 个 public 表面）

## Technical Details

参考 epic.md `### Banner / 信息组件`。`Banner` 已经使用 `BannerStyle` 协议形态（与 Apple `ButtonStyle` 同款），结构良好——本 task 主要是把 `makeBody` 内的字面量替换为 token，**不动**协议形态 + EnvironmentValues 注入路径。

`MessageLevel` 颜色映射（`infoForeground` / `infoBackground` 等）已在 `Sources/CoreDesign/Colors/StatusColors.swift` 中定义，本 task 不调整这些 token 的取值（不在范围内）。

## Dependencies

- v2-tokens 全部 merged（已满足）

## Effort Estimate
- Size: S
- Hours: 4（0.5 天）

## Definition of Done

- [ ] Acceptance Criteria 全部勾选
- [ ] PR opened with 5 字段 description
- [ ] PR review + merge 到 main
