---
name: UnderlinedTabBar 重构
status: open
created: 2026-05-09T22:32:29Z
updated: 2026-05-09T23:21:43Z
github: https://github.com/wxlpp/CoreDesign/issues/24
depends_on: []
parallel: true
conflicts_with: []
---

# Task: UnderlinedTabBar 重构

## Description

把 `Sources/CoreDesign/Components/TabBar/UnderlinedTabBar.swift` (~148 行) 按 v2-tokens 重构。当前文件含 ~5 处带-edge padding 字面量、字号字面量、underline 厚度字面量（`0.5pt` 分隔线、selected underline 高度）。**不使用 `.glassEffect`**。

## Acceptance Criteria

### Token 化
- [ ] 字号字面量（`.font(.subheadline)` 或类似）→ `CoreTypography.bodyMediumFont` / `bodySmallFont`，按视觉决定
- [ ] underline 厚度（譬如 selected indicator 的 `Capsule().frame(height: 2)`）→ `CoreBorderWidth.thick`（2pt）
- [ ] 0.5pt 分隔线（`Rectangle().frame(width: 0.5)`）→ `CoreBorderWidth.hairline`
- [ ] padding `.padding(.horizontal, 12)` 等 ~5 处 → `CoreSpacing.md` 或对应 token
- [ ] spacing `4` / `8` 等 → `CoreSpacing.xs` / `sm`
- [ ] 颜色 token：selected 文字色 `Color.contentPrimary` / unselected `Color.contentSecondary` / underline `Color.accent` 或 `Color.borderFocus`

### Glass 策略
- [ ] **不**使用 `.glassEffect`

### Magic-number 归零
- [ ] `UnderlinedTabBar.swift` 三条 grep 命中 = 0（排除 `#Preview`）

### Build / test / preview
- [ ] `swift build -Xswiftc -warnings-as-errors` 双平台通过
- [ ] `swift test` 退出 0
- [ ] `#Preview` light / dark 双模式 + 选中态切换 + scroll 居中行为无 regression（`ScrollViewReader` 自动滚动逻辑保留不动）

### PR description 5 字段
- [ ] 5 字段全部填写
- [ ] doc-comment 完整性勾选确认（`UnderlinedTabBar<Item, Trailing>` 公开 init + body + `UnderlinedTabItem` 内部 view）

## Technical Details

参考 epic.md `### 控件 chrome`。本组件结构：横向 `ScrollView` + `HStack` of `UnderlinedTabItem` + 固定 `trailing` 视图 + `matchedGeometryEffect` 渲染 underline 切换动画。

**不动**：`ScrollViewReader` 自动滚动到 selected item 的逻辑、`matchedGeometryEffect` 切换动画。

## Dependencies

- v2-tokens 全部 merged（已满足）

## Effort Estimate
- Size: S
- Hours: 4（0.5 天）

## Definition of Done

- [ ] Acceptance Criteria 全部勾选
- [ ] PR opened with 5 字段 description
- [ ] PR review + merge 到 main
