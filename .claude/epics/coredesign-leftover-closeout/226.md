---
name: Sidebar 文档定案 + 三个 issue 关闭
status: open
created: 2026-09-02T15:14:48Z
updated: 2026-09-02T15:28:35Z
github: https://github.com/wxlpp/CoreDesign/issues/226
depends_on: [220, 221, 222, 223, 224, 225]
parallel: false
conflicts_with: []
---

# Task 007 · Sidebar 文档定案 + 三个 issue 关闭

覆盖 PRD FR-10 / FR-11，US-6。

⚠️ **不是「纯记账」**——它背着 FR-10 的 `Sidebar.swift` doc comment 与 `docs/` 定案文，有真实代码 diff。

## 1 · FR-10 · Sidebar 选中态：不改行为，写清楚

**不修改** `SidebarSelectedBackgroundModifier`（`Sidebar.swift:452`）的任何行为。

在 `sidebarSelectedBackground` 的 doc comment 与 `docs/` 相应位置写明：

> 本库侧栏选中态刻意采用**浮层玻璃 + 选中色描边 + 阴影**，**不追随 iOS/macOS 原生的「着色填充、无独立轮廓」**。这是风格决策，不是疏漏。

并记下 #136 提出的对照参考（Files / Reminders / Mail 的侧栏选中样式）供未来重议。

## 2 · FR-11 · 关闭三个 issue

⚠️ **ADR-5 硬约束：每个子项各有独立结论，禁止「已全部处理」这类全称句。** commit message 与 issue 回复不在任何自动校验射程内，是本仓失真的高发地。

### #139（Epic coredesign-native-components）
悬空指针。关闭说明写明：6 个 task #140–#145 全部 CLOSED、`.claude/epics/archived/coredesign-native-components/` 已归档、`v0.4.0` 已打 tag（仓库现已到 `v0.9.0`）。

### #136（视觉终审遗留三条）
逐条：
1. **SurfaceKind 表达力塌缩** → **已修**。附改后映射表与 **iOS 深色 6 / iOS 浅色 5 / macOS 5** 的 distinct 数（**必带平台与外观限定**；⚠️ 若 001 的 α 实测触发了 NFR-2 回改流程，**此处数字随 PRD 推演表同步**，不得沿用本文写死的值），并说明这是「兑现公开 API 承诺」而非「修可见渲染缺陷」——产品代码路径上那六个档位的 `.surface()` 调用点本就为零。
2. **BottomInputBar 无可视路径** → **已修**。附 demo 截图。顺带更正原 issue 漏掉的事实：`.bottomInputBar()` modifier 本就是 public（`:458`）。
3. **Sidebar 选中态** → **按决策不改**，附上第 1 节的理由。

### #115（audit-remediation 遗留四类）
逐条，**作废项必须写明作废来源**：
1. **L10n**：原列 8 处，实为 3 处（4 个字面量）仍成立、已修；`CommentCard` / `StatusRow` 的 4 处**因组件被 #117 整个删除而作废**；`"Toggle Inspector"` 在 `#if DEBUG` 内，非产品路径。
2. **视觉复核**：三项已做（附 006 结论，含如实记录的「无法证实」项）；「Blossom 主题整体观感」**因主题被 #118 移除而作废**。
3. **AGENTS.md 同步**：已修，并新增守卫锁住——不再依赖人的同步义务。
4. **测试覆盖**：两处已处置（附 005 的实际处置方式，若走了改名路线要如实说）。

## 3 · 终检

- `gh issue list --state open` → **0**
- `swift test` + `xcodebuild` iOS 腿双绿，且**两条腿各自**不低于 `baseline.md` 的下限：macOS **454**、iOS **495**
- 三个新守卫的变异自证记录齐备

## Definition of Done

- [ ] `Sidebar.swift` doc comment + `docs/` 定案文已写，行为零改动
- [ ] 三个 issue 全部 closed，**每个子项各有独立结论**
- [ ] 作废项均写明来源（#117 / #118）
- [ ] 关闭说明中未出现「已全部处理」这类全称句
- [ ] 关闭说明中的档位数均带平台与外观限定
- [ ] `gh issue list --state open` 输出为空（贴输出）
