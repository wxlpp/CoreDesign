---
name: a11y 串本地化 + 字面量守卫
status: open
created: 2026-09-02T15:13:11Z
updated: 2026-09-02T15:28:35Z
github: https://github.com/wxlpp/CoreDesign/issues/222
depends_on: []
parallel: true
conflicts_with: []
---

# Task 003 · a11y 串本地化 + 字面量守卫

覆盖 PRD FR-5 / FR-6，US-4。**交付「可本地化」而非「已翻译」**——`Resources/` 目前只有 `en.lproj`。

## 1 · 三处（实为 4 个字面量）入 catalog

| 位置 | 字面量 | 备注 |
|---|---|---|
| `SearchField.swift:94` | `"Search"` | 三元 `placeholder.isEmpty ? "Search" : placeholder`——**只有 fallback 分支要本地化**，`placeholder` 是调用方传入值，库不该翻译 |
| `SearchField.swift:116` | `"Clear \(...)"` | 走位置键如 `"Clear %@"` |
| `SearchField.swift:116` | `"search"` | ⚠️ **插值内层的 fallback**，按行扫描看不见它 |
| `ProgressBar.swift:65` | `"...% complete"` | 位置键如 `"%lld%% complete"`；**百分号在 `.strings` 里需转义**，须验证渲染结果不是字面量 `%%` |

catalog 用 `.strings` / `.stringsdict`——**`.xcstrings` 在 SwiftPM CLI 下不编译**（#100 实证）。

三处均须有测试断言取到的是 catalog 值而非硬编码英文。

## 2 · 守卫

扫 `Sources/CoreDesign/**/*.swift`，跳过 `#if DEBUG` 区块，找 `accessibilityLabel` / `accessibilityValue` / `accessibilityHint` 中**含字符串字面量且不含 `bundle: .module`** 的行，非空即红。

- **必须能看见插值内层的字面量**（上表第 3 行），否则它对自己该防的东西结构性失明。
- 豁免锚定首例：`TagInput.swift:105` `.accessibilityLabel(Text(self.placeholder))`（调用方传入值）。
- ⚠️ **豁免台账落新文件 `docs/a11y-exemptions.json`**，格式参照 `docs/bool-exemptions.json` 但**不得写进该文件**——它正被 002 在 `:27-68` 改动，写进去会打破并发前提。

## 3 · 变异自证

**靶点 = `SearchField.swift:116` 的插值内层 `"search"`**——改回硬编码，守卫必须红。贴失败输出。

⚠️ 打在插值内层而非外层，因为那正是按行扫描最容易失明的形态。

## 参考：非漏网项（勿误改）

`AsyncButton.swift:153`、`Carousel.positionText`、`Rating.accessibilityValueText` 等已走 `String(localized:...bundle: .module)`；`"Toggle Inspector"` 在 `SearchField.swift:236`，位于 `#if DEBUG`（145）–`#endif`（246）之间，非产品路径。

## Definition of Done

- [ ] 4 个字面量全部走 `bundle: .module`，含插值内层
- [ ] `%%` 转义已验证渲染正确
- [ ] 守卫落地，能看见插值内层
- [ ] `docs/a11y-exemptions.json` 新建，含 `TagInput.swift:105` 锚定条目
- [ ] 变异自证（靶点在插值内层）失败输出已贴
- [ ] `swift test` 不低于 `baseline.md` 记录的 454
