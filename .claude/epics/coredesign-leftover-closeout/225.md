---
name: 视觉复核 + 新增合成对照 #Preview
status: open
created: 2026-09-02T15:14:48Z
updated: 2026-09-02T15:28:35Z
github: https://github.com/wxlpp/CoreDesign/issues/225
depends_on: [220, 221, 222]
parallel: false
conflicts_with: []
---

# Task 006 · 视觉复核 + 新增合成对照 #Preview

覆盖 PRD FR-9。

⚠️ **本 task 不是「纯截图」**——它必须在 `Sources/` 新增一个合成对照 `#Preview`，有真实代码 diff，须走正常评审。

## 依赖理由

- **001**：要截的是改后的 `SurfaceKind` 全档。
- **002**：要截 BottomInputBar demo 页。
- **003**：FR-9 含 VoiceOver 冒烟，冒的正是 a11y 串的**读出路径**；003 把该路径从硬编码改成 `.strings` / `.stringsdict` 加载（含 `%lld%%` 转义这种真能读坏的改动）。006 先跑、003 后合，则 007 关闭 #115-2 时引用的冒烟结论覆盖的是**旧代码**。

## 1 · 环境（已就位，但有坑）

iOS 26.4 runtime + `iPhone 17 Pro`。⚠️ 两个已踩过的坑（详见 `baseline.md`）：
- `simctl list devices available` 会列出**磁盘数据已不存在**的设备；boot 失败时 delete + create 重建。
- `xcodebuild ... | tail` 的**退出码来自 `tail`**——`** TEST FAILED **` 会伴随 `exit 0`。**判绿读输出，不读退出码。**

## 2 · 出图

```bash
KEEP_LIBRARY_SNAPSHOTS=1 scripts/run-snapshots.sh
```
keep 模式落**本地 scratch**、`docs/snapshots` 一个字节不动。**禁止跑默认模式**——它 `rm -rf docs/snapshots` 全量重渲染，产生巨大无关 diff。

**唯一要入库的产物**：手工把 BottomInputBar 的**一对**文件（`CoreDesignPreview_BottomInputBar*.png` + 同名 `.json`）拷进 `docs/snapshots/`。`docs/snapshots/` 现有 78 文件 = **39 对**，成对是既有约定，只拷 PNG 会打破它。

自证：`git status --porcelain docs/snapshots` **恰含这一对**，无其他条目。

## 3 · 截图清单（交 `ios-visual-reviewer`）

1. **`SurfaceKind` 全档对照，浅色 + 深色各一套** —— 用现成载体 `SurfaceModifier.swift:188-222` 的 `SurfacePreviewGallery`（已遍历全 10 档）。⚠️ 它是**库内 `#Preview`**，只在 keep 模式出图。
2. **半透明档位与底层档位的合成对照（浅色为重点）** —— ⚠️ **没有现成载体，须新增一个合成 `#Preview`**：`SurfacePreviewGallery` 只把每档平铺在单一 `surfaceCanvas` 底上，产不出「`.floating` 叠 `.content`」这类图。把 `.floating` / `.overlay` / `.panel` 分别叠在 `.canvas` 与 `.content` 之上并排。**守卫测试只担保 Resolved 逐位不同，「浮起来了没有」只能在这里回答。**
3. **001 爆破半径的 6 处 App 宿主消费点**：`ComponentDetail.swift:45,68,77`（`Color.surfacePanel` → 变半透明，**重点**：其父背景若本就是 `surfaceCanvas`，半透明后可能反而更难辨）、`Previews.swift:110` / `ComponentData.swift:310` / `ContentView.swift:49`（`Color.surfaceSidebar` → 换一档背景）。
   ⚠️ `App/` **不被 `swift build` / `swift test` / CI 覆盖**，这 6 处只能靠截图发现问题。
4. **FR-4 的 BottomInputBar demo 页**。
5. **#102 的三个 `#Preview`**：`Sidebar.swift` / `FloatingGlassModifier.swift` / `TelegramGlassButtonModifier.swift`。
6. **#101 `SegmentedControl` thumb 滑动** —— **光栅快照证不了动画在动**。须录屏或多帧截图；若做不到，**如实结案为「无法用现有手段证实」，不得记「已复核」**。

## 4 · VoiceOver 冒烟

`BottomInputBar` / `UnderlinedTabBar` / `Form`。能自动化的部分做，**做不到的部分如实写明做不到**。

## Definition of Done

- [ ] 合成对照 `#Preview` 已新增（代码 diff）
- [ ] 六项截图齐备，浅色 + 深色两套
- [ ] `ios-visual-reviewer` 已出结论，且对「半透明档位是否真的浮起来」有明确回答
- [ ] 无法证实的项（如 thumb 动画）**如实记为「无法证实」**
- [ ] `docs/snapshots` porcelain 恰含一对新增文件
- [ ] 本 task 的结论文字中「可辨 / 好看」这类观感词**只出现在此处**（测试与文档里不得出现）
