---
name: AGENTS.md 同步 + 分歧守卫
status: open
created: 2026-09-02T15:13:50Z
updated: 2026-09-02T15:28:35Z
github: https://github.com/wxlpp/CoreDesign/issues/223
depends_on: []
parallel: true
conflicts_with: []
---

# Task 004 · AGENTS.md 同步 + 分歧守卫

覆盖 PRD FR-7，US-5。

## 背景

`AGENTS.md` 自称是 `CLAUDE.md` 的准确镜像并承诺「未来 `CLAUDE.md` 更新时须同步本文件」。**该义务已被违反过一次，且违反物是一句已知为假的断言。**

## 1 · 修掉唯一的实质分歧

`AGENTS.md:42` 段末仍保留：

> 重度使用 iOS 26 的 `.glassEffect()`；`LightButtonStyle` 会按 `colorScheme` 分支：暗色用 `glassEffect`，亮色用柔和阴影代替。

`CLAUDE.md` 已在 #41 收尾时把这句标注为「**后半句实测为假**」并删除，同时补了两段：`:42` 的更正段（`Button/styles/` 下没有任何样式直接调 `.glassEffect`，也没有任何一个读 `colorScheme`）与 `:44` 的「`.glassEffect` 的真实调用面在组件与 modifier 层」。

**处置**：删掉 `AGENTS.md:42` 的假句，补上 `CLAUDE.md:42,44` 两段。

## 2 · 自动守卫

规范化 diff：消除已知的定位替换后，残余差异非空即红。

**已知允许的差异（走显式白名单，不得用宽松正则吃掉一切）**：
- banner 行（AGENTS.md 顶部的「本文件是 `CLAUDE.md` 的 Codex 版镜像」段）
- 标题行 `# CLAUDE.md` ↔ `# AGENTS.md`
- 定位句「本文件为 Claude Code (claude.ai/code) ……」↔「本文件为 Codex ……」

## 3 · 变异自证

往 `AGENTS.md` 正文任意插入一行 `CLAUDE.md` 没有的内容 → 守卫必须红。贴失败输出。

## Definition of Done

- [ ] `AGENTS.md:42` 假句已删，两段更正已补
- [ ] 规范化 diff 残余差异为空（贴输出）
- [ ] 守卫落地，白名单为显式枚举
- [ ] 变异自证失败输出已贴
- [ ] `swift test` 不低于 `baseline.md` 记录的 454
