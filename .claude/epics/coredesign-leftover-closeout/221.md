---
name: BottomInputBar 提 public + demo 接通 + 裁决/豁免处置
status: open
created: 2026-09-02T15:13:11Z
updated: 2026-09-02T15:28:35Z
github: https://github.com/wxlpp/CoreDesign/issues/221
depends_on: []
parallel: true
conflicts_with: []
---

# Task 002 · BottomInputBar 提 public + demo 接通

覆盖 PRD FR-4，US-3。**独立成 task/PR**——它要推翻一条成文裁决，不是「守卫报错后顺手改绿」。

## 1 · 代码改动

- `Sources/CoreDesign/Components/BottomInputBar/BottomInputBar.swift:19` `struct BottomInputBar: View` → `public`，其 `public init` 与公开成员按本仓规范补中英双语 doc comment。
- `App/Sources/ComponentData.swift:219-221` 的 `BottomInputBarPreview` 从占位 `Text` 改为**真实可交互宿主**：`@State` 文本、非空 suggestions、可触发 `onSubmit`、能看到 suggestions 显隐。
- `App/Sources/Previews.swift:165-166` 的 `#Preview("BottomInputBar")` 同步改为同一宿主。

> ⚠️ 默认快照模式只保留 `Previews.swift` 驱动的 `CoreDesignPreview_*`，库内 `#Preview` 会被 `find -delete`——**必须走 `Previews.swift` 才有快照**。

## 2 · 三样真实工作量（本 task 的主体）

### 2.1 推翻成文裁决
`docs/component-contract.md:1278-1291` 的终审 C1 裁决把 BottomInputBar「按 AD-2 排除出登记表」，理由是「`struct BottomInputBar: View` 没有 `public` 修饰符」，并把 `placeholder` 归为「落在 FR-4 定义域之外」（由 `ComponentTextParamGuard.knownFunctionSideBareText` 的固定集合断言盯着）。

**提 public 正是该裁决自己给出的两条出路之一**（「给它一个可登记的 public 类型表面」）——方向合法，但须一并改写：裁决文本、判定法走查（kind / nativeProtocol / needsExtensionPoint）、`placeholder` 的正式分类。

### 2.2 `docs/bool-exemptions.json:27-68` 的 7 条豁免
`autoShowSuggestions` / `wandEnabled` / `sendEnabled` / `showMenuButton` / `isRunning` / `showShuffleButton` / `autoFocus`。

- **1 条必须改写**：`:28` `autoShowSuggestions` 的 reason 直接引用「BottomInputBar 按公约 AD-2 排除出登记表（**struct 无 public 修饰符**）」——提 public 后该句失真。
- 其余 6 条 reason 不依赖该前提，但 struct 提 public 后这 7 个 Bool 会形成**第二张 public 参数面**（modifier 侧已有一张），须评估是否新增豁免条目。

### 2.3 跨仓移交通知
`component-contract.md:1290-1291` 指明该事项已移交 `oh-my-story` 仓（close-out.md 移交清单，issue #50 承接）。**本 task 只负责在 CoreDesign 侧完成 + 在 issue 里点名通知，不代改 oh-my-story。**

## 3 · 守卫族处置

新 public 类型很可能触发多个固定集合断言，逐个报出即逐个处置——这是**范围内工作，不是意外**：
`ComponentRegistryGuard` / `ComponentContractStructureGuard` / `ComponentExtensionPointGuard` / `ReachableTypeRegistryGuard` / `ComponentTextParamGuard` / `BoolParameterScanner`。

## 4 · 文档

`docs/components/bottom-input-bar.md` 与 `docs/component-contract.md` 相应条目。

## 并发安全说明

- 本 task 是 `docs/bool-exemptions.json:27-68` 的**唯一改动者**。003 的 a11y 豁免台账已钉死落**新文件** `docs/a11y-exemptions.json`，不碰此文件。
- `BottomInputBar.swift` 现有 3 处 a11y（`:150` / `:166` / `:179`）**已全部走 `bundle: .module`**，与 003 无交集。新增 public 面若引入新 a11y 串，须同样走 `bundle: .module`（否则会撞 003 的守卫）。

## Definition of Done

- [ ] `BottomInputBar` 为 public，doc comment 齐备
- [ ] demo 宿主可输入 / 可提交 / 可见 suggestions 显隐，两处（`ComponentData` + `Previews`）一致
- [ ] C1 裁决文本 + 判定法走查 + `placeholder` 分类已改写
- [ ] `:28` 豁免 reason 已改写；第二张参数面已评估并记录结论
- [ ] 全部守卫绿（贴输出）
- [ ] `oh-my-story#50` 已点名通知（不代改）
- [ ] `swift test` 不低于 `baseline.md` 记录的 454
