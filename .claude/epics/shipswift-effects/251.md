---
name: 转场簇（16 种）
status: decomposed
created: 2026-09-03T00:16:11Z
updated: 2026-09-03T19:59:48Z
github: https://github.com/wxlpp/CoreDesign/issues/251
depends_on: [244, 245, 246, 247] # 跨 epic：shipswift-foundation 地基四件
parallel: false # 本体不直接实现，由三个子 task 承载
conflicts_with: []
---

# Task: 转场簇（16 种）

## Description

按 CoreDesign 公约重新实现 ShipSwift `SWTransition.swift` 的 **16 种**转场，
落 `CoreDesignEffects`。CoreDesign 目前**没有任何 `Transition` 实现**，
调用方只能用 SwiftUI 内建的 `.slide` / `.opacity`。

## Acceptance Criteria

- [ ] 16 种转场可用：blur / flip / rotate3D / swoosh / boing / skid / move / iris /
      wipe / blinds / clock / flicker / filmExposure / snapshot / glare / dissolve
- [ ] 以 `Transition` 协议实现 + `extension Transition where Self == …` 的静态成员暴露，
      支持 `.transition(.iris)` 点语法
- [ ] 含参重载（如 `.wipe(angle:)` / `.move(angle:distance:)` / `.dissolve(cellSize:)`）
      **算同一种**——计数单位是"一种 transition"不是"一个静态成员"
- [ ] **Reduce Motion 降级**：位移/旋转类降级为淡入淡出，测试可证
- [ ] 各有 `#Preview` + `docs/components/*.md`
- [ ] `EffectsColorLiteralGuard` 零违规

## Technical Details

⚠️ **`Transition` 静态成员不是 `View` / `ViewModifier`**，`ComponentRegistryGuard` 的
`PublicTypeCollector` 原本**结构上扫不到它们** ⇒ 它们不进组件登记表。
⚠️ **本句原写「按 AD-4 的裁决手工维护并在盲区台账留痕」——AD-4《下游连锁二》
逐字推翻了它**（「24 条量级的手工表必漂，正是 G-7 记在案的失效形态」），随 #244/#246 回改：
16 个转场走 **扩展成员扫描器**（`#246` 已把 `PublicTypeCollector` 扩到 extension 成员，
认 `extension Transition where Self == …` 上的静态成员），**登记进
`docs/component-registry.json` 的 `entryPoints` 数组**（`target` / `host` / `member` / `notes`
四字段），由 `ExtensionEntryPointGuard` 做双向差集：漏登记与幽灵条目两个方向都判红。
⚠️ **登记落点不再"待定"**：AD-4 第 6 轮收敛为「AD-2 原样适用」，路线 a 与它的
`docs/effects-registry.json` 一并撤回，`entryPoints` 就在 `component-registry.json` 里。
⚠️ **含参重载算同一条**：扫描器按 `Host.member` 去重，与本文件 AC「计数单位是
『一种 transition』不是『一个静态成员』」同一口径。

mask reveal 类（iris / wipe / blinds / clock / dissolve）的 `Shape` 数学是上游最有价值的
部分，可直接借鉴。

## ⚠️ 本 task 已拆解，**不要直接对它派实现**

按下方 decompose 指令拆成三个子 task（2026-09-03T19:59:48Z）：

| 子 task | 组 | 种数 | 转场 |
|---|---|---|---|
| #266 | 滤镜类 | 4 | `blur` / `filmExposure` / `snapshot` / `flicker` |
| #267 | 3D 与弹性 | 6 | `flip` / `rotate3D` / `swoosh` / `boing` / `skid` / `move` |
| #268 | mask reveal | 6 | `iris` / `wipe` / `blinds` / `clock` / `glare` / `dissolve` |

4 + 6 + 6 = **16**，与本文件 AC 的 16 种逐个对应、无重叠无遗漏。

⚠️ **本文件内部有一处出入，拆解时按分组指令走**：下方 Technical Details 写
「mask reveal 类（iris / wipe / blinds / clock / dissolve）」是 **5 个**，而 decompose
分组把 `glare` 也划进该组是 **6 个**。只有按 6 个算三组才凑满 16 ⇒ 已按分组指令归入 #268。
若实现中发现 `glare` 的数学更接近滤镜类，可调整归属，但**必须同步改本文件**，不要让两处继续打架。

⚠️ **三个子 task 的并行冲突面**：都写 `docs/component-registry.json` 的 `entryPoints`
数组 ⇒ 各自只追加自己那组的条目、不重排他人条目，合并顺序由主会话串行处理
（与本 epic 既有的「`ComponentData.swift` 的写入由 007 串行合并」同一类处置）。

---

⚠️ **decompose 必须再拆**：16 种按「滤镜类（blur/filmExposure/snapshot/flicker）/
3D 与弹性（flip/rotate3D/swoosh/boing/skid/move）/ mask reveal（iris/wipe/blinds/clock/
glare/dissolve）」三组，每组一个子 task。

## Dependencies

`shipswift-foundation` 的 001 ~ 004。

## Effort Estimate
- Size: XL
- Hours: 20–28

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
