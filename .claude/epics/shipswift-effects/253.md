---
name: 文本与展示动效（4 个）
status: closed
created: 2026-09-03T00:16:49Z
updated: 2026-09-04T03:54:41Z
github: https://github.com/wxlpp/CoreDesign/issues/253
depends_on: [244, 245, 246, 247] # 跨 epic：shipswift-foundation 地基四件
parallel: true
conflicts_with: []
---

# Task: 文本与展示动效（4 个）

## Description

落 `TypewriterText` / `AnimatedMeshGradient` / `BeforeAfterSlider` / `ParticleTransition`。

## Acceptance Criteria

- [ ] 4 个 API 可用，命名裸名
- [ ] **`BeforeAfterSlider` 的 Bool 参数不照搬**：上游的 `showLabels: Bool` 撞 J-1
      ⇒ 改语义枚举（如 `labels: .shown(before:after:)` / `.hidden`），
      或申请豁免并抬棘轮基线（本 epic 预算 ≤ 2 条）
- [ ] **`BeforeAfterSlider` 的 "Before" / "After" 默认文案**必须是
      `LocalizedStringResource` / `LocalizedStringKey`（公约 A 类，`ChromeTextLiteralGuard` 判）
- [ ] **`AnimatedMeshGradient` 不自带调色板**：9 色 × 2 组全部由调用方传入或取 `.tint`
      （上游默认 indigo/blue/cyan 是硬编码色，撞 `EffectsColorLiteralGuard`）
- [ ] **Reduce Motion**：`TypewriterText` 降级为直接显示完整文本；
      `AnimatedMeshGradient` 冻结在某一帧；`BeforeAfterSlider` 停止自动摆动但保留拖拽
- [ ] **`BeforeAfterSlider` 的触控目标测试**在 `CoreDesignEffectsTests` 内同形态实现
      ⚠️ **不得加进 `TouchTargetTests`**——那会让 `CoreDesignTests` 依赖新 target，
      判红 `shipswift-foundation` task 002 立的 NFR-5② 判据
- [ ] 各有 `#Preview` + `docs/components/*.md`

## Dependencies

`shipswift-foundation` 的 001 ~ 004。

## Effort Estimate
- Size: L
- Hours: 12–18

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
