---
name: 庆祝与处理中动效（4 个）+ 可注入能耗 environment
status: open
created: 2026-09-03T00:16:49Z
updated: 2026-09-03T00:20:45Z
github: https://github.com/wxlpp/CoreDesign/issues/252
depends_on: [244, 245, 246, 247] # 跨 epic：shipswift-foundation 地基四件
parallel: true
conflicts_with: []
---

# Task: 庆祝与处理中动效（4 个）+ 可注入能耗 environment

## Description

落 `Confetti` / `ScanningOverlay` / `GlowSweep` / `LightSweep`，并建立 **NFR-7 的可注入
`EnvironmentValues`** —— 后者是本 epic 与 `shipswift-shaders` 之间的一条**跨 epic 契约**。

## Acceptance Criteria

### ⚠️ AD-4 复判闸的触发点（随 #244 追加）

本 task 是**第一个产出登记面 Effects 类型**的 task（`ScanningOverlay` 正是 AD-4 的
样本 1）⇒ AD-4 的复判闸挂在这里，**不是 246**。
⚠️ 挂 246 是**落空的**：246 落地时真实 Effects 件是 **0 个**（本 task 的
`depends_on` 就含 246；task 250 的 8 个微交互按 AD-2 排除、进不了主数组）
⇒「前两条非空」在空数组上**真空为真 ⇒ 绿**。
而 `246.md` 自己就记录过同款失效形态（「新守卫在 0 个文件上必绿」）
——同一份裁决在别处防住了它、在闸门上又踩了一次。

- [ ] **前两个进登记面的 Effects 件，如实记录 b 侧 notes 字符数**与它走的是哪个出口
      （≥3 具名候选支 / 「已穷尽」支）。
      ⚠️ **不要求"至少一件是纯装饰件"**——那是不可判定条款：「纯装饰件」无定义
      （公约定义的是纯装饰**层**）、「能走已穷尽支」是判定的**产出**而非可选输入，
      且无兜底 ⇒ 会变成「那就跳过闸门」，即闸门被自己的前提静默关掉。
- [ ] **机器痕迹（fail-closed）**：`bSideNotesChars` 字段 + 断言
      「主数组条目数 ≥ 1 时前两条必须 > 0；**条目数 = 0 时闸门状态记为『未触发』，
      禁止据此认定任何一侧成立**」。
- [ ] **改判条件**：实测单价 **≥ `a_fixed/P + 每件成本`**（三个锚下
      **825 / 1215 / 1625**）⇒ **改判 a** 并建差集守卫；否则维持暂定 b。
      ⚠️ 绝对阈值写死在这里，不要求读者代公式——AD-4 上一版正是因为公式量纲错
      （`P` 是件数、`每件成本` 是字符数）而写成了恒假条件。
- [ ] **承接**：由本 task 的 DoD 触发，开 `AD-4 复判` issue，
      结论以**修订 AD-4 正文 + 同步 246 / 251 / 252** 的形式落地。


- [ ] 4 个 API 可用：`myView.confetti(trigger:)` / `ScanningOverlay { }` /
      `GlowSweep { }` / `LightSweep { }`
- [ ] **Confetti 粒子颜色默认取自调用方 `.tint`**，不自带彩虹色板
- [ ] **Reduce Motion**：Confetti 不播放粒子，降级为一次淡入淡出的静态庆祝层
- [ ] **burst 结束后驱动它的 `TimelineView` 停止调度或被移除**
      （⚠️ 上游用 `TimelineView(.animation)`（`SWConfetti.swift:171,435`），不是 Timer /
      DisplayLink——不要去找一个不存在的泄漏源）
- [ ] **NFR-7 后台 / 低电量行为可测**：`ProcessInfo.isLowPowerModeEnabled` 与 `scenePhase`
      在测试里不可直接切换 ⇒ 做成**可注入的 `EnvironmentValues`**（默认从系统读），
      测试注入伪值断言渲染行为。⚠️ **不接受"或文档声明"**
- [ ] **⚠️ 这两个可注入键必须 `public`**：`shipswift-shaders` 的 17 个 `colorEffect` 要
      `import CoreDesignEffects` 复用它们，而 `@Entry` 默认 internal ⇒ 不显式 `public`，
      B-2 用不了，跨 epic 契约断掉
- [ ] **可选**：若 `shipswift-shaders` 的 B-2 先于 task 007 完成，**NFR-1 性能 harness
      在本 task 落地**而非 007——避免 17 个 colorEffect 全落完才第一次跑性能闸
- [ ] 各有 `#Preview` + `docs/components/*.md`；`EffectsColorLiteralGuard` 零违规

## Dependencies

`shipswift-foundation` 的 001 ~ 004。
**本 task 阻塞 `shipswift-shaders` 的 B-2**（可注入 environment 键）。

## Effort Estimate
- Size: L
- Hours: 12–18

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing（含注入伪值的后台/低电量断言）
- [ ] 两个 environment 键确认为 `public`
- [ ] Code reviewed
