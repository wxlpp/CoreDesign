---
name: 收尾——预览宿主 / 快照 / 性能基准 / probe / 文档与署名
status: open
created: 2026-09-03T00:17:45Z
updated: 2026-09-03T00:20:45Z
github: https://github.com/wxlpp/CoreDesign/issues/256
depends_on: [250, 251, 252, 253, 254, 255]
parallel: false
conflicts_with: []
---

# Task: 收尾——预览宿主 / 快照 / 性能基准 / probe / 文档与署名

## Description

Epic A 的收尾 task。⚠️ **它同时是 `shipswift-shaders` B-4 的前置**——
`ComponentData.swift` 的串行写入窗口与 NFR-1 性能基准脚本都在这里定。

## Acceptance Criteria

### 预览宿主（⚠️ 并行冲突面，必须串行）
- [ ] `App/Sources/ComponentData.swift`（32.8K 画廊 registry）合并全部 40 个 API 单位的条目
      ⚠️ `semi-mobile-components` epic 已把它与 `Previews.swift` 列为冲突面并专设串行阶段；
      task 001~006 只产出片段，**由本 task 统一合并**
- [ ] `scripts/run-preview.sh` 能构建并展示全部已落地 API 单位

### 快照排除策略
- [ ] `scripts/run-snapshots.sh` + `App/Tests/SnapshotTests.swift` 会渲染**所有链入模块的
      `#Preview`**，而 `Confetti` / `ParticleTransition` / `AnimatedMeshGradient` 产出
      **非确定 PNG**，且默认模式会 `rm -rf docs/snapshots` 重生成
      ⇒ **必须定排除策略**并落地，`run-snapshots.sh` 不因非确定 PNG 而漂移

### NFR-1 性能基准（一次性基建，`shipswift-shaders` B-4 复用同一脚本）
- [ ] 基准脚本落地且**可重复跑**
- [ ] Confetti 默认粒子数不掉帧；`NetworkGraph` 在声明的节点上限内不掉帧，
      **均由脚本断言而非人工抽测**
- [ ] ⚠️ **须钉运行环境**：PRD NFR-1 钉的是"iPhone 15 满帧"，而 CI 只有 macOS runner 与
      iOS Simulator——**Simulator 上的 GPU frame-time 无意义**，在 CI 上绿不等于 NFR-1 过。
      ⇒ 脚本须在**真机**（iPhone 15 或钉死的替代机型）执行，至少随 epic 收尾跑一次并留证；
      CI 上的数值只作趋势参考
- [ ] ⚠️ **若 `shipswift-shaders` B-2 先于本 task 完成，harness 应已在 task 003 落地**
      （两边都写了这个可选项，须同步）

### probe 实质调用点
- [ ] **probe 覆盖 Effects / Charts 的 40 个 API 单位所对应的全部类型**的 nonisolated 调用点
      （`shipswift-foundation` task 004 只做接线）
      ⚠️ 措辞刻意不写"全部**公开**值类型"——那是自指的：漏写 `public` 的类型压根不算"公开"，
      probe 自然不覆盖它，FR-5 就没人查。**按 API 单位清单点名**，漏 `public` 会在
      probe 编译期直接炸出来。
      ⚠️ **分流规则见 `EffectsNonisolatedUsage.swift` 文件头**（#260 终审 Important-2）：**值类型 / 配置类型 / 数据入参**进 `*NonisolatedUsage.swift`；**View / modifier / style 类**进 `PublicVisibility.swift`（`@MainActor`）——本包开了 `.defaultIsolation(MainActor.self)`，View 的 `init` 与 modifier 函数**天然是 MainActor 隔离的**，从 `nonisolated func` 里构造必然编译失败。「按 API 单位清单点名」= **每个单位在这两处之一必有引用**，不是全部塞进 nonisolated 文件。
- [ ] `cd scripts/downstream-probe && swift build` 绿 ⇒ NFR-4 的 MainActor 隔离契约
      在这两个 target 上真的被验证

### 文档与署名
- [ ] 每个 API 单位有 `docs/components/*.md`
- [ ] **`docs/README.md` 索引**已挂，**落点按 `shipswift-foundation` task 001 的 AD-4 裁决**
      （主索引 vs `## 生成预览图` 之后另起小节）
- [ ] **`ACKNOWLEDGEMENTS.md` 追加 ShipSwift 条目**，区分"参考算法思路"与"较大段落移植"两档
      ⚠️ **文件骨架由 `shipswift-foundation` task 006 建**，本 task 不新建
- [ ] **`docs/reachable-type-registry.json`** 已登记本 epic 新增的可达类型
      ⚠️ **不写"`ReachableTypeRegistryGuard` 绿"——那对新 target 是空真**：
      实测该守卫只查 schema / 与登记表不相交 / 参数名唯一 / 全 C 四条，**不扫源码**；
      深度扫描在 `ComponentExtensionPointGuard.swift:20` 走
      `ComponentRegistryGuard.coreDesignSources` = `Sources/CoreDesign`
      ⇒ 少登记一条守卫照样绿。**处置**：引用 task 006 扩根后的扫描结果；
      若最终未扩根，则诚实写明"登记完整性靠人工 + PR 评审，守卫只查 schema"

### Epic 级验收
- [ ] 40 个 API 单位中 **≥ 37** 项"可用"（四条 AND：编译 / 过守卫 / 有 Preview 且进画廊 / 有文档）
- [ ] CI 四条腿全绿；`CoreDesign` 公开 API 表面 diff 为空（工具同 task 002 所钉）
- [ ] Bool 豁免基线净增 **≤ 2 条**（全局预算 3 = Effects 2 + Shaders 1），
      每条有书面理由，按 `scripts/bool-exemptions-ratchet.sh` 流程抬基线
- [ ] **NFR-5② 沿用**：`swift package describe … CoreDesignTests … target_dependencies`
      仍恰为 `["CoreDesign"]`（Epic A 加测试时最容易破坏它）

## Dependencies

本 epic 的 task 001 ~ 006 全部完成。

## Effort Estimate
- Size: L
- Hours: 14–22

## Definition of Done
- [ ] Code implemented
- [ ] CI 四条腿全绿
- [ ] 性能基准在真机跑过并留证
- [ ] Code reviewed
