---
name: 组件机械改名扫尾并删除弃用别名
status: open
created: 2026-07-21T15:23:19Z
updated: 2026-07-22T00:00:00Z
github: https://github.com/wxlpp/CoreDesign/issues/121
depends_on: [119, 120]
parallel: false
conflicts_with: [120, 122, 124]
---

# Task: 组件机械改名扫尾并删除弃用别名

## Description

把全部组件与 modifier 的调用点从旧 token 名迁到新名，然后删掉 Task #119 留下的弃用别名。

这一段是**机械的、由编译器驱动的**：Task #119 的 `@available(*, deprecated, renamed:)` 会为每个旧名调用点产生一条 warning，逐条消除即可。与 Task #122 的判断密集型工作拆开，是因为二者的验证方式完全不同——本任务的完成判据是「warning 清零、别名删净」，006 的判据是「每个档位选择都经过确认」。

## Acceptance Criteria

### 改名迁移

- [ ] Sources 内 **42 处**需改名的 `.coreFont` 调用迁到新名（`bodySmall` 16、`bodyMedium` 14、`bodyLarge` 6、`titleSmall` 4、`titleMedium` 2）。`caption` 11 / `captionMono` 4 **不在本任务范围**——名字未变、无 warning，归 Task #122 的同名换值处置
- [ ] `Sidebar.swift:157,411` 的 `CoreRadius.mediumPlus` 迁到新档位
- [ ] `primerVerticalPadding(for:)` 的调用点（若有）迁走
- [ ] 覆盖范围：`Components/`（Avatar / AvatarGroup / Badge / Banner / Tag / ListRow / Form / SearchField / SegmentedControl / Sidebar / UnderlinedTabBar / Toast / ProgressBar / ProgressIndicator / CheckBox / StateLabel / BottomInputBar / CoreMenuButton / AsyncButton / 4 个 Button style / StatusLevel）、`Layout/FlowLayout.swift`、`Shape/StarShape.swift`、`Modifier/`（`CoreFontModifier.swift` 除外——归 Task #119 重写）、`Utils/ColorExtension.swift`
- [ ] **18 处旧 `*Font` static var 调用迁到新用法**：`App/Sources/` 17 处（`Previews.swift` / `ComponentData.swift` / `ComponentDetail.swift` / `ContentView.swift`）+ Sources 内 1 处（`Avatar.swift:55`，`titleLargeFont.weight(.bold)`）——**CI 不构建 App**（`ci.yml` 无 xcodegen / run-preview 步骤），这条断链对本任务之前的所有判据都不可见，漏掉会在 Task #125 才爆
- [ ] **`Tests/` 内 3 处 `.coreFont` 调用**（`DynamicTypeLayoutTests`）迁到新名——否则删别名后断链

### 别名清理

- [ ] `CoreTypography` 的 9 个 `@available(deprecated)` 改名别名删除，以及 Task #119 为 10 个旧 `*Font` static var 所留的 deprecated 定义
- [ ] `CoreRadius.smallPlus` / `mediumPlus` 删除
- [ ] `CoreControlMetrics.primerVerticalPadding(for:)` 删除

> ⚠️ `scripts/downstream-probe` 是独立 SwiftPM 包，**`swift build` / `swift test` 不覆盖它**，只有 CI 的 Downstream API probe job 会跑。任何删除或改名公开符号的改动都必须同步它，否则本地全绿而 CI 红。Issue #117 就在这里翻过车（引用了随组件删除的 `StatusResult`）。

### 从 #119 交接的陈旧注释（迁移调用点时顺手修正）

- [ ] `Avatar.swift:52-55` 的「本就不纳入 Dynamic Type，故用固定 Font 值」在别名新实现下**已不成立**——`titleLargeFont` 现在随 Dynamic Type 缩放，且档位由 32pt semibold 变为 28pt regular
- [ ] `Badge.swift:59` 的文档注释引用了已删除的 `bodySmallTracking`

**height 换值（24/28/32/40/48 → 28/32/44/50/56）产生的同类失实注释**——数字全错，设计理由本身仍成立，只需改数：

- [ ] `CoreMenuButton.swift:63` 「`.large` = 40pt」→ 50pt
- [ ] `CoreMenuButton.swift:114` 「`height(for: .large)` = 40pt」→ 50pt（该值真实决定按钮外框尺寸）
- [ ] `CircularGlassButtonStyle.swift:19` 「40pt，落在 `CoreControlMetrics.height` 的 24/28/32/40/48 序列内」——整个旧序列已不存在
- [ ] `CircularGlassButtonStyle.swift:24` 「`.controlSize(.regular)` 时会静默得到 40pt」→ 44pt
- [ ] `CircularGlassButtonStyle.swift:58` 「`.large`，40pt」→ 50pt
- [ ] `AsyncButton.swift:77` 「决定 40pt 直径」→ 50pt
- [ ] `BottomInputBar.swift:225-226` 「`height(for: .extraLarge)` 是 48pt」→ 56pt（「刻意保留 44」的设计理由仍成立，仅数字失实）

### 验证

- [ ] `swift build` 无 deprecation warning
- [ ] `swift build` / `swift test` 通过，CI 全部命令通过（含 xcodebuild iOS 腿）
- [ ] **`scripts/run-preview.sh` 能构建预览宿主**——CI 不覆盖这条，须手动验证；Task #118 有此 AC，中间任务不能把它丢了

## Technical Details

本任务**不做视觉判断**——不评估圆角是否该翻倍、高度是否该从 32 变 44，那些是 Task #122 的事。这里只保证「旧名不再被引用、别名已删净、树是绿的」。

改名迁移与 Task #122 的逐点重审必须分开，否则执行者会在同一遍里既做机械替换又做设计判断，结果通常是机械替换做完了而设计判断被跳过。

## Dependencies

依赖 Task #119（别名与新名）与 004（语义色定案）。

与 004 在 `Components/Button/` 上冲突（`ButtonRoleStyleRole` 归 004），与 006 / 008 在全部组件文件上冲突。

## Effort Estimate
- Size: M
- Hours: 6

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
