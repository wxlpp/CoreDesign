---
name: 组件机械改名扫尾并删除弃用别名
status: open
created: 2026-07-21T15:23:19Z
updated: 2026-07-21T16:05:00Z
github: (will be set on sync)
depends_on: [003, 004]
parallel: false
conflicts_with: [004, 006, 008]
---

# Task: 组件机械改名扫尾并删除弃用别名

## Description

把全部组件与 modifier 的调用点从旧 token 名迁到新名，然后删掉 Task 003 留下的弃用别名。

这一段是**机械的、由编译器驱动的**：Task 003 的 `@available(*, deprecated, renamed:)` 会为每个旧名调用点产生一条 warning，逐条消除即可。与 Task 006 的判断密集型工作拆开，是因为二者的验证方式完全不同——本任务的完成判据是「warning 清零、别名删净」，006 的判据是「每个档位选择都经过确认」。

## Acceptance Criteria

### 改名迁移

- [ ] Sources 内 **42 处**需改名的 `.coreFont` 调用迁到新名（`bodySmall` 16、`bodyMedium` 14、`bodyLarge` 6、`titleSmall` 4、`titleMedium` 2）。`caption` 11 / `captionMono` 4 **不在本任务范围**——名字未变、无 warning，归 Task 006 的同名换值处置
- [ ] `Sidebar.swift:157,411` 的 `CoreRadius.mediumPlus` 迁到新档位
- [ ] `primerVerticalPadding(for:)` 的调用点（若有）迁走
- [ ] 覆盖范围：`Components/`（Avatar / AvatarGroup / Badge / Banner / Tag / ListRow / Form / SearchField / SegmentedControl / Sidebar / UnderlinedTabBar / Toast / ProgressBar / ProgressIndicator / CheckBox / StateLabel / BottomInputBar / CoreMenuButton / AsyncButton / 4 个 Button style / StatusLevel）、`Layout/FlowLayout.swift`、`Shape/StarShape.swift`、`Modifier/`（`CoreFontModifier.swift` 除外——归 Task 003 重写）、`Utils/ColorExtension.swift`
- [ ] **18 处旧 `*Font` static var 调用迁到新用法**：`App/Sources/` 17 处（`Previews.swift` / `ComponentData.swift` / `ComponentDetail.swift` / `ContentView.swift`）+ Sources 内 1 处（`Avatar.swift:55`，`titleLargeFont.weight(.bold)`）——**CI 不构建 App**（`ci.yml` 无 xcodegen / run-preview 步骤），这条断链对本任务之前的所有判据都不可见，漏掉会在 Task 009 才爆
- [ ] **`Tests/` 内 3 处 `.coreFont` 调用**（`DynamicTypeLayoutTests`）迁到新名——否则删别名后断链

### 别名清理

- [ ] `CoreTypography` 的 9 个 `@available(deprecated)` 改名别名删除，以及 Task 003 为 10 个旧 `*Font` static var 所留的 deprecated 定义
- [ ] `CoreRadius.smallPlus` / `mediumPlus` 删除
- [ ] `CoreControlMetrics.primerVerticalPadding(for:)` 删除

### 验证

- [ ] `swift build` 无 deprecation warning
- [ ] `swift build` / `swift test` 通过，CI 全部命令通过（含 xcodebuild iOS 腿）
- [ ] **`scripts/run-preview.sh` 能构建预览宿主**——CI 不覆盖这条，须手动验证；Task 002 有此 AC，中间任务不能把它丢了

## Technical Details

本任务**不做视觉判断**——不评估圆角是否该翻倍、高度是否该从 32 变 44，那些是 Task 006 的事。这里只保证「旧名不再被引用、别名已删净、树是绿的」。

改名迁移与 Task 006 的逐点重审必须分开，否则执行者会在同一遍里既做机械替换又做设计判断，结果通常是机械替换做完了而设计判断被跳过。

## Dependencies

依赖 Task 003（别名与新名）与 004（语义色定案）。

与 004 在 `Components/Button/` 上冲突（`ButtonRoleStyleRole` 归 004），与 006 / 008 在全部组件文件上冲突。

## Effort Estimate
- Size: M
- Hours: 6

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
