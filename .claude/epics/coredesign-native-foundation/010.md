---
name: 文档、CI 与版本收尾
status: open
created: 2026-07-21T15:23:19Z
updated: 2026-07-21T15:23:19Z
github: (will be set on sync)
depends_on: [009]
parallel: false
conflicts_with: []
---

# Task: 文档、CI 与版本收尾

## Description

把地基更换的结果落到文档、CI 与版本上。这里包含一件容易被当成「以后再说」的事：`CLAUDE.md` 与 `AGENTS.md` 里有整节关于主题系统与 CoreGradient 的说明，Phase 1 之后全部变成错误指引——而这个仓库重度依赖 agent 按这两份文件工作，过期指令有实际成本。

## Acceptance Criteria

### 设计依据文档

- [ ] `docs/PRIMER_VERSION.md` 删除，替换为 `docs/DESIGN-FOUNDATION.md`
- [ ] `DESIGN-FOUNDATION.md` 记录 Apple HIG 依据与各 token 取值理由，含 Task 004 提供的 accent 衍生族档位取值理由

### 破坏性变更清单

- [ ] `docs/BREAKING-CHANGES.md` 新增本次条目，含三节：
      - **删除的公开符号**：6 个组件、`StatusResult`、`CoreGradient`、`Blossom` trait、`CoreRadius.smallPlus/mediumPlus`、`CoreControlMetrics.primerVerticalPadding`、`CoreTypography` 的 `*LineSpacing`/`*Tracking`/`fixedFont` 等
      - **改名的 token**：`CoreTypography` 的 9 个改名档位（旧名 → 新名）、10 个旧 `*Font` static var、以及 `caption` / `captionMono` 的**同名换语义**与 `subheadline` 净新增
      - **同名换值**（独立成节，探针对此失明）：`CoreRadius` 各档、`CoreControlMetrics.height` 各档、语义色指向变更，逐项列旧值 → 新值
- [ ] 替代物要等 `0.4.0` 才有的条目明确标注「将于 0.4.0 提供」，不留空
- [ ] 说明下游可评估从 `0.2.0` 直接跳到 `0.4.0`

### 项目指令文件

- [ ] `CLAUDE.md`：删除/重写「主题系统（Package Traits）」「渐变 token 层（CoreGradient）」整节，以及「两种构建模式都需保持绿」「新增 colorset 后必须 `swift package clean`」等约定；「分层色彩系统」一节按 Task 004 的新映射改写
- [ ] `AGENTS.md`：同上范围的对应段落
- [ ] 两份文件中不再出现 Blossom / CoreGradient / Primer 作为现行约定

### 组件文档与快照

- [ ] `docs/README.md` 组件索引更新（移除 6 个已删组件，组件计数修正）
- [ ] `scripts/run-snapshots.sh` 重新生成快照，`docs/snapshots/` 与新观感一致

### CI 与版本

- [ ] CI 无 `--traits Blossom` 与 `-skip-testing:CoreDesignTests/BlossomAssetTests` 残留（Task 002 已做，本任务复核）
- [ ] 下游编译探针（`scripts/downstream-probe`）跑通，并在文档中明示其覆盖边界——探针只能发现删除与改名符号，对同名换值系统性失明，不得以探针通过代替逐点确认
- [ ] `README.md` 的版本 pin 更新为 `0.3.0`
- [ ] **打 `0.3.0` tag 并发 release**——本 epic 的交付物是 `0.3.0`，10 个任务里此前无人负责发版动作

## Technical Details

BREAKING-CHANGES 的「同名换值」一节是本任务最容易做浅的部分。下游对 `CoreRadius.small` 从 3 变 6 是编译无感的，若不逐项列出，升级后只会表现为「界面看着不太对」而无从定位。该节内容来自 Task 006 的迁移映射表。

## Dependencies

依赖 Task 009——文档必须以**视觉终审后的终态数值**写就。Task 006 的迁移映射表是 BREAKING-CHANGES「同名换值」一节的输入；若 009 的修复回改了 token 值，本任务须以修复后的值为准。

## Effort Estimate
- Size: L
- Hours: 8

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
