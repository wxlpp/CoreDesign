---
name: 删除 Blossom trait 与 CoreGradient
status: open
created: 2026-07-21T15:23:19Z
updated: 2026-07-22T00:00:00Z
github: https://github.com/wxlpp/CoreDesign/issues/118
depends_on: []
parallel: true
conflicts_with: []
---

# Task: 删除 Blossom trait 与 CoreGradient

## Description

移除 Blossom 主题能力。「强品牌色渐变皮肤」与「跟随系统外观」是两个方向的产品目标，同时维护会让后续每个 token 改动与新组件的验证矩阵翻倍。

`CoreGradient` 随之删除：它存在的唯一理由是让 Blossom 拥有渐变而默认主题退化为纯色，Blossom 移除后三个 token 退化成「纯色包一层 `AnyShapeStyle`」，是净负担。

本任务放在 token 重铸之前：`#if Blossom` 分流恰好压在 `ColorGrade` / `SurfaceColors` / `InteractionColors` 上，正是 Task #119 / 004 要改的文件，先删可避免在将被删除的分支上做迁移。

## Acceptance Criteria

- [ ] `Package.swift` 的 `traits:` 声明删除
- [ ] `Sources` 内 8 处 `#if Blossom` 删除（`ColorGrade:12`、`CoreGradient:22/38/54`、`InteractionColors:10`、`SurfaceColors:50/62/74`），保留默认分支行为
- [ ] `Resources.xcassets/blossom-brand/*` 与 `blossom-canvas/*` 色板删除
- [ ] `Tokens/CoreGradient.swift` 与 `Tokens/CoreGradient+Preview.swift` 删除
- [ ] `Tests/CoreDesignTests/BlossomColorDivergenceTests.swift` 删除（含 `:54` 的 `#if Blossom`）
- [ ] `Tests/CoreDesignTests/CoreDesignTests.swift` 中**两个 suite 都删**：`BlossomAssetTests`（`:21-22`）与 **`CoreGradientTests`（`:50-66`，含 `:62` 的 `#if Blossom` 与渐变退化断言）**——渐变断言在后者，不在前者，只删 `BlossomAssetTests` 会留下引用已删符号的 suite
- [ ] CI（`.github/workflows/ci.yml`）：删 **`:30` 的 `matrix: mode: [default, blossom]` 及 Build/Test 步骤里的整套 if/else 骨架（`:35-40`、`:52-58`）**——只删两行 `--traits Blossom` 会留下残缺的条件分支；另删 `:116` 的 `-skip-testing:CoreDesignTests/BlossomAssetTests` 及其成因注释、`:139-142` downstream-probe 的 Blossom 说明
- [ ] `App/project.yml:13` 的 `traits: ["Blossom"]` 与上方注释删除，`App/CoreDesignPreview.xcodeproj` 经 xcodegen 重新生成（pbxproj:447-448 含 `traits = (Blossom,)`）
- [ ] `grep -rn "#if Blossom" Sources Tests App` 返回 0 行（基线：分流 10 处 = Sources 8 + Tests 2；grep 行数为 11，多出的一行是 `BlossomColorDivergenceTests.swift:10` 注释里的字面提及，该文件整体删除）
- [ ] `grep -rn "CoreGradient" Sources Tests App` 返回 0 行
- [ ] `swift build` / `swift test` 通过，预览宿主可构建

## Technical Details

trait 删除后若 `App/project.yml` 未同步，SwiftPM 解析会直接报未知 trait，预览宿主无法生成——而 Task #125 的视觉评审依赖它。这条不是或有风险，是确定会发生的破坏。

`CoreGradient` 在 `Sources` 内的唯一外部调用点是 `CommentCard`，由 Task #117 删除；若 001 尚未合入，本任务需自行处理该引用。

## Dependencies

无前置。与 Task #117 可并行（触及 `App/` 的不同文件）。

## Effort Estimate
- Size: M
- Hours: 4

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
